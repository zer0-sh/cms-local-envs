DB_NAME=drupal
DB_USER=drupal
DB_PASSWORD=secret_change_me
DB_HOST=db
DB_PORT=3306
DB_DRIVER=mysql

DRUPAL_HASH_SALT=change_me_generate_a_random_string

# Trusted host patterns (regex, escape dots)
DRUPAL_TRUSTED_HOST_PATTERNS=^localhost$

DRUPAL_ENV=development
